// NPM calls a reusable code a package. 

// Modules/dependicies/package are terms used interchangebly

// no quality control in npmjs.com

// no. of weekly download high = useful one.

// **************************************

// npm - global command, comes with node

// local dependency - use it only in this particluar project
// npm i <package name>


//  We can also install dependency as global dependency - we can use it in any project
// npm install -g <packageName>

// package.json - manifest file (stores important info about project/package)
// manual approach (create package.json in the root, create properties etc)
// automatic approach - npm init
// npm init -y (everything is setup as default)

